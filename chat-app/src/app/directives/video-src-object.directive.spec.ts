import { VideoSrcObjectDirective } from './video-src-object.directive';

describe('VideoSrcObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoSrcObjectDirective();
    expect(directive).toBeTruthy();
  });
});
